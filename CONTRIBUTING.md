# Contributing

1. Fork the repo and create a branch
2. Add your utility with tests
3. Run `npm test` (JS) or `cargo test` (Rust)
4. Open a PR with a clear description

## Setup

```bash
# JS
cd packages/js && npm install

# Rust
cd packages/rust && cargo build
```
